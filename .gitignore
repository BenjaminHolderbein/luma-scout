# Secrets / local config
.env

# Local run artifacts
*.log
candidates.json
rank_prompt.txt
ranked.json
__pycache__/
*.pyc
.venv/

# NOTE: state/seen.json IS committed (it's the persistence layer for the
# cloud routine). Nothing sensitive in it — only Luma event ids + dates.
